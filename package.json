{
  "name": "da1me",
  "devDependencies": {
    "esmify": "^2.1.1",
    "standard": "*"
  },
  "scripts": {
    "build": "npx browserify src/index.js -p esmify > scripts/bundle.js",
    "dev": "standard && npm run build && node app.js",
    "fixme": "standard --fix"
  },
  "dependencies": {
    "browserify": "^17.0.1",
    "express": "^5.1.0",
    "jquery": "^3.7.1",
    "wordcloud": "^1.2.2"
  },
  "standard": {
    "ignore": [
      "**/bundle.js",
      "scripts/bundle.js",
      "**/*_ok.js",
      "**/*.py",
      "**/hinos/*"
    ]
  }
}
